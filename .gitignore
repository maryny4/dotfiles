# Secrets — never commit. Real values live only in ~/.config, not here.
hypr-rdp/config.toml
hypr-rdp/cert.pem
hypr-rdp/key.pem
hypr-rdp/.tls.lock

# Local state that must not be tracked
nvim/lazy-lock.json.bak
*.orig
*.swp

# Build artifacts — rebuild from patches/ instead
*.pkg.tar.zst
