# efi_pstore: persist panic messages to EFI NVRAM so they survive the reboot.
# amd64_edac: register the memory controller so DRAM errors are counted at all —
# without it nothing watches memory, which is the prime freeze suspect here.
efi_pstore
amd64_edac
